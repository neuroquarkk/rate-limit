{
    "name": "rate-limit",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "bun run --hot src/index.ts",
        "format": "prettier . --write",
        "check": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "@types/express": "^5.0.3",
        "prettier": "^3.6.2"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "express": "^5.1.0",
        "redis": "^5.6.0"
    }
}
